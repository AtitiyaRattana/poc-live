/* Dart Side (Flutter Code) */
import 'package:flutter/services.dart';

class FlutterBridge {
  static const platform = MethodChannel('channel_face_recognition');

  // static Future<dynamic> callLiveness() async {
  //   try {
  //     final result = await platform.invokeMethod('callLiveness');
  //     return result;
  //   } catch (e) {
  //     return e;
  //   }
  // }

  // Rest of the Code
}
